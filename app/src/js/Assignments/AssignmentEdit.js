import React from 'react';

import Typography from '@material-ui/core/Typography';

const AssignmentEdit = (props) => {
    return (
      <Typography paragraph>Assignment Edit {props.id}</Typography>
    )      
  }

export default AssignmentEdit;