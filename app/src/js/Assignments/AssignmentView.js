import React from 'react';

import Typography from '@material-ui/core/Typography';

const AssignmentView = (props) => {
    return (
      <Typography paragraph>Assignment View {props.id}</Typography>
    )      
  }

export default AssignmentView;