import React, { Component } from 'react';

import Typography from '@material-ui/core/Typography';

const ClassesIndex = (props) => {
    return (
        <Typography>
            Classes Index Placeholder for teacher {props.teacherId}
        </Typography>
    )
}

export default ClassesIndex;
